[package]
name = "tokencount"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "stdin loop that counts tokens for vim-tokencount"

[dependencies]
tiktoken-rs = "0.6"
base64 = "0.22"

[profile.release]
opt-level = 3
lto = true
strip = true
