[package]
name = "tokencount"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "stdin loop that counts Claude tokens for vim-tokencount"

[dependencies]
claude-tokenizer = "0.3"
base64 = "0.22"

[profile.release]
opt-level = 3
lto = true
strip = true
